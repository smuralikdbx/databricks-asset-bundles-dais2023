# The main pipeline for demo_for_dabs_python
resources:
  pipelines:
    demo_for_dabs_python_pipeline:
      name: demo_for_dabs_python_pipeline
      target: demo_for_dabs_python_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: /Workspace/${workspace.file_path}/src
